export default function VoiceAgent() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: "var(--bg)",
        color: "var(--ink)",
      }}
    >
      <h1 style={{ fontSize: 32, fontWeight: 800 }}>Diya Voice Agent</h1>
    </div>
  );
}
